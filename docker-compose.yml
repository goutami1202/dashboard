
version: "3.8"
services:
  fintech-web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/home/app/uploads
      - ./outputs:/home/app/outputs
    environment:
      - PORT=5000
      - HOST=0.0.0.0
    restart: unless-stopped
